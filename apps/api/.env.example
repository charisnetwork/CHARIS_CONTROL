# Never commit values. Production requires all variables below.
DATABASE_URL=
JWT_SECRET=
PORT=4000
REDIS_URL=
# Comma-separated browser origins, for example https://control.example.com
CORS_ALLOWED_ORIGINS=
# NODE_ENV=production refuses to start without JWT_SECRET and CORS_ALLOWED_ORIGINS.

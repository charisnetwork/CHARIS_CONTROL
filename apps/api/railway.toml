[build]
buildCommand = "npm run build"

[deploy]
# Keep schema migration as a deliberate deployment step. Normal API startup must
# never invoke `prisma db push`, especially after launch.
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
